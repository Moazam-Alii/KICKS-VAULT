# backend/mint.py

def mint_nft_on_solana(wallet_address, sku):
    print(f"Minting NFT for SKU {sku} to wallet {wallet_address}")
    # Fake transaction hash
    return f"tx_mint_{sku}_{wallet_address}"
